---
phase: 07-improve-ui
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - dashboard/components/home/stats-cards.tsx
  - dashboard/components/navigation/mobile-menu.tsx
  - dashboard/app/page.tsx
  - dashboard/app/admin/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Home page shows total assets count and last sync timestamp"
    - "Home page shows 3-5 recent asset updates"
    - "Admin sidebar hidden on mobile, hamburger menu visible instead"
    - "Mobile menu opens as slide-out drawer with admin navigation links"
    - "Search functionality preserved on home page"
  artifacts:
    - path: "dashboard/components/home/stats-cards.tsx"
      provides: "Dashboard statistics display cards"
      contains: "StatsCards"
    - path: "dashboard/components/navigation/mobile-menu.tsx"
      provides: "Mobile hamburger menu with Sheet"
      contains: "MobileMenu"
    - path: "dashboard/app/page.tsx"
      provides: "Home page with stats and search"
      contains: "StatsCards"
    - path: "dashboard/app/admin/layout.tsx"
      provides: "Responsive admin layout with mobile menu"
      contains: "hidden md:block"
  key_links:
    - from: "dashboard/app/page.tsx"
      to: "dashboard/lib/api.ts"
      via: "getStats() call"
      pattern: "getStats"
    - from: "dashboard/app/admin/layout.tsx"
      to: "dashboard/components/navigation/mobile-menu.tsx"
      via: "MobileMenu component import"
      pattern: "MobileMenu"
---

<objective>
Add stats cards to home page and implement mobile-responsive admin navigation.

Purpose: Complete Phase 7 UI improvements with dashboard statistics and mobile-first responsive navigation.
Output: Home page with stats/search, admin section with responsive sidebar and hamburger menu on mobile.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-improve-ui/07-RESEARCH.md
@.planning/phases/07-improve-ui/07-01-SUMMARY.md

# Relevant source files
@dashboard/app/page.tsx
@dashboard/app/admin/layout.tsx
@dashboard/components/ui/card.tsx
@dashboard/components/ui/sheet.tsx
@dashboard/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stats cards component and update home page</name>
  <files>dashboard/components/home/stats-cards.tsx, dashboard/app/page.tsx</files>
  <action>
1. Create `dashboard/components/home/stats-cards.tsx`:
   - Import Card, CardHeader, CardTitle, CardContent from @/components/ui/card
   - Import Link from next/link
   - Accept props: totalAssets, lastSyncAt, recentAssets (from DashboardStats type)
   - Render 3 cards in responsive grid: `grid gap-4 md:grid-cols-2 lg:grid-cols-3`

Cards:
- **Total Assets**: Show count as large number (text-3xl font-bold)
- **Last Sync**: Format timestamp with toLocaleString('vi-VN') or show "Never" if null
- **Recent Updates**: List 3-5 recent assets with name and version, link to /assets/[id]

Example:
```tsx
export function StatsCards({ totalAssets, lastSyncAt, recentAssets }: DashboardStats) {
  return (
    <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3 mb-8">
      <Card>
        <CardHeader className="pb-2">
          <CardTitle className="text-sm font-medium text-muted-foreground">Total Assets</CardTitle>
        </CardHeader>
        <CardContent>
          <p className="text-3xl font-bold">{totalAssets}</p>
        </CardContent>
      </Card>
      {/* ... other cards */}
    </div>
  );
}
```

2. Update `dashboard/app/page.tsx`:
   - Fetch stats using getStats() from @/lib/api
   - Render StatsCards component above search section
   - Keep existing search functionality intact
   - Handle loading and error states for stats fetch

Use useEffect to fetch stats on mount. Display stats above the search bar.
Preserve existing debounced search logic.
  </action>
  <verify>
Start dashboard and verify home page:
```bash
cd dashboard && npm run dev
```
Visit http://localhost:3000:
- Stats cards visible above search bar
- Total Assets shows a number
- Last Sync shows timestamp or "Never"
- Recent Updates shows asset list (if any)
- Search still works as before
  </verify>
  <done>Home page displays stats cards with total assets, last sync, recent updates; search still functional</done>
</task>

<task type="auto">
  <name>Task 2: Create mobile menu and update admin layout</name>
  <files>dashboard/components/navigation/mobile-menu.tsx, dashboard/app/admin/layout.tsx</files>
  <action>
1. Create `dashboard/components/navigation/mobile-menu.tsx`:
   - Add 'use client' directive (Sheet requires client-side state)
   - Import Sheet, SheetContent, SheetTrigger, SheetHeader, SheetTitle from @/components/ui/sheet
   - Import Button from @/components/ui/button
   - Import Menu icon from lucide-react
   - Import Link from next/link

Structure:
```tsx
'use client';

import { Menu } from 'lucide-react';
import { Sheet, SheetContent, SheetTrigger, SheetHeader, SheetTitle } from '@/components/ui/sheet';
import { Button } from '@/components/ui/button';
import Link from 'next/link';

export function MobileMenu() {
  return (
    <Sheet>
      <SheetTrigger asChild>
        <Button variant="ghost" size="icon">
          <Menu className="h-5 w-5" />
          <span className="sr-only">Open menu</span>
        </Button>
      </SheetTrigger>
      <SheetContent side="left">
        <SheetHeader>
          <SheetTitle>Admin</SheetTitle>
        </SheetHeader>
        <nav className="flex flex-col gap-4 mt-4">
          <Link href="/admin" className="text-lg hover:underline">Overview</Link>
          <Link href="/admin/channels" className="text-lg hover:underline">Channels</Link>
          <Link href="/admin/jobs" className="text-lg hover:underline">Jobs</Link>
        </nav>
      </SheetContent>
    </Sheet>
  );
}
```

2. Update `dashboard/app/admin/layout.tsx`:
   - Add responsive classes to sidebar: `hidden md:block` (hide on mobile, show on md+)
   - Add mobile header with MobileMenu component: `md:hidden` (show on mobile only)
   - Add top padding to main content on mobile to account for fixed mobile header
   - Keep existing sidebar navigation and Bull Board link

Structure:
```tsx
import Link from 'next/link';
import { MobileMenu } from '@/components/navigation/mobile-menu';

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex min-h-screen">
      {/* Mobile header - visible only on mobile */}
      <div className="md:hidden fixed top-14 left-0 right-0 border-b bg-background p-4 z-10">
        <MobileMenu />
      </div>

      {/* Desktop sidebar - hidden on mobile */}
      <nav className="hidden md:block w-56 border-r bg-muted/40 p-4">
        {/* existing sidebar content */}
      </nav>

      {/* Main content - extra top padding on mobile for fixed header */}
      <main className="flex-1 p-6 pt-20 md:pt-6">{children}</main>
    </div>
  );
}
```

Note: top-14 accounts for root layout header height. Adjust if needed.
  </action>
  <verify>
1. Start dashboard: `cd dashboard && npm run dev`
2. Desktop test (width >= 768px):
   - Navigate to http://localhost:3000/admin
   - Should see sidebar with Overview, Channels, Jobs links
   - No hamburger menu visible
3. Mobile test (resize browser to < 768px or use DevTools):
   - Sidebar should disappear
   - Hamburger menu icon should appear
   - Click hamburger - Sheet drawer slides in from left
   - Navigation links work from mobile menu
  </verify>
  <done>Admin section has responsive sidebar (desktop) and hamburger menu drawer (mobile)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 7 UI improvements:
- Stats cards on home page (total assets, last sync, recent updates)
- Navigation header on all pages with Admin link
- Mobile-responsive admin sidebar with hamburger menu
  </what-built>
  <how-to-verify>
1. **Home page (http://localhost:3000):**
   - [ ] Stats cards visible (Total Assets, Last Sync, Recent Updates)
   - [ ] Search bar still works
   - [ ] "Admin" link in header navigates to admin section

2. **Admin section (http://localhost:3000/admin):**
   - [ ] Desktop (>768px): Full sidebar visible
   - [ ] Mobile (<768px): Hamburger menu visible, sidebar hidden
   - [ ] Mobile: Clicking hamburger opens slide-out navigation
   - [ ] All navigation links work (Overview, Channels, Jobs)

3. **Consistent navigation:**
   - [ ] Header with "Vesper" logo visible on all pages
   - [ ] "Vesper" logo links to home page
   - [ ] "Admin" link visible in header on all pages
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. StatsCards component exists and renders 3 cards
2. Home page shows stats above search
3. MobileMenu component exists with Sheet-based drawer
4. Admin layout uses responsive classes (hidden md:block for sidebar)
5. Mobile hamburger menu opens slide-out navigation
6. All links work correctly across desktop and mobile
</verification>

<success_criteria>
- Home page displays total assets count, last sync time, recent asset updates
- Search functionality preserved on home page
- Admin sidebar visible on desktop (>=768px)
- Hamburger menu visible on mobile (<768px) that opens navigation drawer
- Navigation works consistently on all pages and devices
</success_criteria>

<output>
After completion, create `.planning/phases/07-improve-ui/07-02-SUMMARY.md`
</output>
