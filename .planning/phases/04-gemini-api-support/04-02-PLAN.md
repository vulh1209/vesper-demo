---
phase: 04-gemini-api-support
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/services/query/intent.ts
  - src/services/query/test-providers.ts
autonomous: true

must_haves:
  truths:
    - "Intent extraction uses provider factory instead of hardcoded OpenAI"
    - "Both Gemini and OpenAI produce consistent structured output"
    - "Fallback activates when primary provider fails"
    - "Graceful degradation returns search intent when LLM unavailable"
  artifacts:
    - path: "src/services/query/intent.ts"
      provides: "Updated intent extraction using provider factory"
      exports: ["extractIntent", "extractIntentSafe"]
    - path: "src/services/query/test-providers.ts"
      provides: "Test script for both LLM providers"
      exports: []
  key_links:
    - from: "src/services/query/intent.ts"
      to: "src/config/llm.ts"
      via: "import getModelWithFallback"
      pattern: "import.*getModelWithFallback.*from.*config/llm"
    - from: "src/services/query/intent.ts"
      to: "generateObject"
      via: "AI SDK structured output"
      pattern: "generateObject"
---

<objective>
Integrate Gemini provider into intent extraction and create provider testing script.

Purpose: Enable the query system to use Gemini as LLM provider with automatic fallback to OpenAI.

Output: Updated intent.ts using provider factory, plus test-providers.ts for validation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-gemini-api-support/04-RESEARCH.md
@.planning/phases/04-gemini-api-support/04-01-SUMMARY.md

# Current implementation to update
@src/services/query/intent.ts
@src/services/query/types.ts
@src/config/llm.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update intent extraction to use provider factory</name>
  <files>src/services/query/intent.ts</files>
  <action>
Modify `src/services/query/intent.ts`:

1. **Update imports:**
   - Remove: `import { openai } from '@ai-sdk/openai';`
   - Add: `import { getModelWithFallback } from '../../config/llm.js';`

2. **Update extractIntent function:**
   - Replace `model: openai('gpt-4o-mini')` with `model: getModelWithFallback()`
   - Update JSDoc comment to reflect multi-provider support:
     - Remove: "Uses OpenAI gpt-4o-mini"
     - Add: "Uses configured LLM provider (Gemini or OpenAI) with automatic fallback"
     - Update cost note: "~$0.00005/query with Gemini, ~$0.00015/query with OpenAI"

3. **Update extractIntentSafe function:**
   - In the catch block, update the fallback intent to use `intent: 'search'` instead of `intent: 'unknown'`
   - This matches the graceful degradation pattern from research (treat query as potential asset name)
   - Keep the console.error logging

4. **Keep everything else unchanged:**
   - SYSTEM_PROMPT stays the same
   - QueryIntentSchema stays the same
   - All exports stay the same

The key change is replacing the hardcoded `openai('gpt-4o-mini')` with the factory call.
  </action>
  <verify>
Run `npm run build` - TypeScript compiles
Run `npx tsx -e "import { extractIntentSafe } from './src/services/query/intent'; console.log('import ok');"` - module loads without error
  </verify>
  <done>
intent.ts uses getModelWithFallback() instead of hardcoded openai(), TypeScript compiles
  </done>
</task>

<task type="auto">
  <name>Task 2: Create provider test script</name>
  <files>src/services/query/test-providers.ts</files>
  <action>
Create `src/services/query/test-providers.ts`:

1. **Purpose:**
   Test both LLM providers with Vietnamese and English queries to verify:
   - Both produce valid QueryIntent objects
   - Vietnamese queries are handled correctly
   - Response times are comparable

2. **Structure:**
   ```typescript
   import 'dotenv/config';
   import { google } from '@ai-sdk/google';
   import { openai } from '@ai-sdk/openai';
   import { generateObject } from 'ai';
   import { QueryIntentSchema } from './types.js';

   const TEST_QUERIES = [
     'Ho ly moi nhat?',           // Vietnamese: latest version
     'Show me all sounds',         // English: category list
     'lich su boss theme',         // Vietnamese: version history
   ];

   async function testProvider(name, model, query) {
     // Run generateObject with QueryIntentSchema
     // Return result with timing
   }

   async function main() {
     // Test OpenAI if OPENAI_API_KEY set
     // Test Gemini if GOOGLE_GENERATIVE_AI_API_KEY set
     // Compare results side by side
   }
   ```

3. **Output format:**
   - Provider name, query, parsed intent
   - Response time in ms
   - Side-by-side comparison for same queries

4. **Add npm script:**
   In package.json, add: `"test:providers": "tsx src/services/query/test-providers.ts"`

Note: This tests the raw providers directly (not through the factory) to compare behavior.
  </action>
  <verify>
Run `npm run build` - TypeScript compiles
If API keys available: `npm run test:providers` shows output for configured providers
  </verify>
  <done>
test-providers.ts exists and can be run with `npm run test:providers`
  </done>
</task>

<task type="auto">
  <name>Task 3: Validate integration with existing test</name>
  <files>src/services/query/test.ts</files>
  <action>
Run the existing query test to verify the integration works end-to-end:

1. **No code changes to test.ts** - it already tests the full pipeline

2. **Run test with OpenAI:**
   Set OPENAI_API_KEY only, run `npm run test:query`
   Verify intent extraction works

3. **If Gemini key available, run with Gemini:**
   Set GOOGLE_GENERATIVE_AI_API_KEY only, run `npm run test:query`
   Verify same queries produce similar intents

4. **Document any differences** in processing time or intent confidence

The test confirms that swapping providers doesn't break the query pipeline.
  </action>
  <verify>
Run `npm run test:query` with at least one provider configured - test completes without errors
  </verify>
  <done>
Existing query test passes with updated intent.ts using provider factory
  </done>
</task>

</tasks>

<verification>
1. `npm run build` - TypeScript compiles without errors
2. `npm run test:query` - existing test passes with provider factory
3. `npm run test:providers` - new test script runs (if API keys available)
4. Code review: intent.ts imports from config/llm.ts, not @ai-sdk/openai directly
</verification>

<success_criteria>
- intent.ts uses getModelWithFallback() from config/llm.ts
- Hardcoded openai() import removed from intent.ts
- test-providers.ts tests both providers with Vietnamese queries
- npm run test:query passes with at least one provider configured
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-gemini-api-support/04-02-SUMMARY.md`
</output>
